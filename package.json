{
  "name": "mcaree-guestbook-api",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "bundle": "rm -f guestbook_uploader_lambda.zip ; zip guestbook_uploader_lambda.zip -r *.js *.json node_modules/"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "aws-multipart-parser": "^0.2.1",
    "path": "^0.12.7",
    "uuid": "^9.0.0"
  }
}
